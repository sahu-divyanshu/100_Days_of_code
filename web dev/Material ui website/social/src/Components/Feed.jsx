import { makeStyles } from "@mui/styles";
const useStyles = makeStyles((theme)=>({
    
}))
const Feed = () => {
    const classes = useStyles();
  return (
    <div>
      
    </div>
  )
}

export default Feed
